---
// 必須インポート
import LowerPageHeader from '../../../components/ui/LowerPageHeader.astro';

// 画像
import JapanMapSpImage from '../../../assets/images/access/hero/sp.svg';
import JapanMapTabletUpImage from '../../../assets/images/access/hero/tablet-up.svg';
---

<section class="access-hero">
  <!-- ページヘッダー -->
  <div class="page-header-container">
    <LowerPageHeader titleEn="ACCESS" titleJa="アクセス" />
  </div>
  <div class="access-hero__content">
    <h1 class="access-hero__heading">
      首都圏からすぐ<br />
      息をのむ自然の中へ
    </h1>
    <p class="access-hero__description">
      首都圏からわずかな距離で、息をのむような自然に囲まれた当ホテルへお越しください。緑豊かな山々、透き通るような湖、清々しい空気が、日常からの完全な逃避を提供します。手つかずの自然の中で過ごす時間は、都会の喧騒から解放され、心身ともにリフレッシュする貴重な瞬間です。自然と直接触れ合うことで、新たな発見と感動があなたを待っています。この美しい場所で、忘れられない思い出を作りましょう。
    </p>
    <address class="access-hero__address">
      <div class="access-hero__address-block">
        <p class="access-hero__postal-code">〒401-0332</p>
        <p class="access-hero__address-text">
          山梨県南都留郡富士河口湖町西湖９９７
        </p>
      </div>
      <p class="access-hero__phone">TEL 0555-82-2922</p>
    </address>
  </div>
  <figure class="access-hero__map">
    <picture class="access-hero__map-image">
      <!-- デスクトップ（1024px以上）→ 最高解像度版 -->
      <source
        media="(min-width: 1024px)"
        srcset={JapanMapTabletUpImage.src.toString()}
      />
      <!-- タブレット（744px以上）→ 中解像度版 -->
      <source
        media="(min-width: 744px)"
        srcset={JapanMapTabletUpImage.src.toString()}
      />
      <!-- スマホ（744px未満）→ 最小解像度版 -->
      <img
        src={JapanMapSpImage.src}
        alt="東京・名古屋・大阪からのアクセスマップ"
        class="access-hero__map-img"
      />
    </picture>
  </figure>
</section>

<style lang="scss">
  @import '../../../styles/variables';
  @import '../../../styles/functions';
  @import '../../../styles/mixins';

  // ============================================================
  // TODO: accessを適切な名前に変更してスタイルを記述
  // ============================================================

  .access-hero {
    position: relative;
    padding-top: spx(110);
    padding-bottom: spx(72);
    overflow-x: hidden;
    @include tablet-up {
      padding-top: ppx(280) * 1.2;
      padding-bottom: ppx(231) * 1.2;
    }
    @include desktop-up {
      padding-top: ppx(280);
      padding-bottom: ppx(231);
    }

    .page-header-container {
      position: relative;
      z-index: 2;
      @include tablet-up {
        margin-left: ppx(233 * 1.2);
      }
      @include desktop-up {
        margin-left: ppx(233);
      }
    }

    // コンテンツコンテナ
    &__content {
      position: relative;
      z-index: 2;
      @include tablet-up {
      }
      @include desktop-up {
      }
    }

    // 見出し
    &__heading {
      color: var(--blue_6, #054965);
      font-family: 'Zen Kaku Gothic New';
      font-size: spx(52);
      font-style: normal;
      font-weight: 400;
      line-height: 150%;
      letter-spacing: spx(2.6);
      margin-top: spx(90);
      width: spx(600);
      margin-inline: auto;
      @include tablet-up {
        font-size: ppx(70 * 1.2);
        letter-spacing: ppx(3.5 * 1.2);
        margin-top: ppx(107 * 1.2);
        width: auto;
        margin-inline: unset;
        margin-left: ppx(233 * 1.2);
      }
      @include desktop-up {
        font-size: ppx(70);
        letter-spacing: ppx(3.5);
        margin-top: ppx(107);
        margin-left: ppx(233);
      }
    }

    // 説明文
    &__description {
      color: var(--blue_6, #054965);
      font-family: 'Zen Kaku Gothic New';
      font-size: spx(30);
      font-style: normal;
      font-weight: 400;
      line-height: 180%;
      width: spx(600);
      margin-top: spx(40);
      margin-inline: auto;
      @include tablet-up {
        font-size: ppx(26 * 1.2);
        letter-spacing: ppx(-0.52 * 1.2);
        width: 25vw;
        margin-top: ppx(26 * 1.2);
        margin-inline: unset;
        margin-left: ppx(233 * 1.2);
      }
      @include desktop-up {
        font-size: ppx(26);
        letter-spacing: ppx(-0.52);
        width: ppx(984);
        margin-top: ppx(26);
        margin-left: ppx(233);
      }
    }

    // 住所ブロック
    &__address {
      color: var(--blue_6, #054965);
      font-family: 'Zen Kaku Gothic New';
      font-size: spx(30);
      font-style: normal;
      font-weight: 400;
      line-height: 160%;
      letter-spacing: spx(-0.6);
      width: max-content;
      margin-left: auto;
      margin-right: spx(29);
      margin-top: spx(614);
      @include tablet-up {
        text-align: right;
        font-size: ppx(33 * 1.2);
        line-height: 180%;
        letter-spacing: ppx(1.65 * 1.2);
        margin-top: 5vw;
        margin-right: ppx(150 * 1.2);
      }
      @include desktop-up {
        font-size: ppx(33);
        letter-spacing: ppx(1.65);
        margin-top: ppx(703);
        margin-right: ppx(150);
      }
    }

    &__address-block {
      @include tablet-up {
        display: flex;
        gap: 1em;
      }
      @include desktop-up {
      }
    }

    // 郵便番号
    &__postal-code {
      @include tablet-up {
      }
      @include desktop-up {
      }
    }

    // 住所テキスト
    &__address-text {
      @include tablet-up {
      }
      @include desktop-up {
      }
    }

    // 電話番号
    &__phone {
      @include tablet-up {
      }
      @include desktop-up {
      }
    }

    // マップコンテナ
    &__map {
      position: absolute;
      z-index: 1;
      right: 0;
      top: spx(445);

      @include tablet-up {
        top: 0;
      }
      @include desktop-up {
      }
    }

    // マップ画像コンテナ
    &__map-image {
      @include tablet-up {
      }
      @include desktop-up {
      }
    }

    // マップ画像
    &__map-img {
      display: block;
      width: 100%;
      @include tablet-up {
      }
      @include desktop-up {
      }
    }
  }
</style>
